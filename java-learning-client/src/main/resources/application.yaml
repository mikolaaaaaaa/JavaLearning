server:
  port: 8080
feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic
spring:
  mvc:
    hiddenmethod:
      filter:
        enabled: true
    view:
      prefix: /login
  cloud:
    openfeign:
      okhttp:
        enabled: true
  security:
    user:
      name: ivanov@example.com
      password: 1